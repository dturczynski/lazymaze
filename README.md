# Lazy Maze

A 2d game, where, as the player, you must reach the portal to win.
Avoid red dots.
Use arrow keys to move.

## Dependencies

- SFML
- CMake
- C++20

## How to build and run

```console
cmake -B build
cmake --build ./build
./build/lazymaze/run
```
